rules_version = '2';

// All access to the parameters collection is gated through the backend Admin SDK.
// Direct client reads/writes are denied.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
